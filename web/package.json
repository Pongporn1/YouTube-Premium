{
  "name": "mytube-private-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check video-utils.js && node --check api/video.js && node --check api/youtube-client.js && node --check api/feed.js && node --check api/search.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
